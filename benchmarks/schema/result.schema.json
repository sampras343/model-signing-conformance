{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/sampras343/model-signing-conformance/benchmarks/schema/result.schema.json",
  "title": "BenchmarkResult",
  "description": "A single benchmark result entry produced by the harness. Multiple entries are collected in a JSON array.",
  "type": "object",
  "required": ["client", "scenario", "operation"],
  "additionalProperties": false,
  "properties": {
    "status": {
      "enum": ["ok", "skipped"],
      "description": "Whether this entry represents a completed run or a skipped scenario. Absent means 'ok' for backward compatibility."
    },
    "skip_reason": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable reason why the scenario was skipped (present only when status is 'skipped')"
    },
    "client": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]*$",
      "description": "Adapter identifier, e.g. 'python', 'go'"
    },
    "client_version": {
      "type": "string",
      "minLength": 1,
      "description": "Client library/binary version if reported by adapter capabilities"
    },
    "scenario": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*$",
      "description": "Name field from the scenario YAML file"
    },
    "operation": {
      "enum": ["hash", "sign", "verify"],
      "description": "Which operation was timed"
    },
    "parameters": {
      "type": "object",
      "description": "Exact parameter values used for this run",
      "required": ["model_size_bytes", "file_count", "method"],
      "additionalProperties": false,
      "properties": {
        "model_size_bytes": { "type": "integer" },
        "file_count":       { "type": "integer" },
        "method":           { "oneOf": [{ "enum": ["key", "certificate", "sigstore"] }, { "type": "null" }] },
        "hash_algorithm":   { "type": ["string", "null"] },
        "serialization":    { "type": ["string", "null"] },
        "chunk_size":       { "type": ["integer", "null"] },
        "max_workers":      { "type": ["integer", "null"] },
        "shard_size":       { "type": ["integer", "null"] }
      }
    },
    "results": {
      "type": "object",
      "required": ["repeat", "times_ms", "mean_ms", "min_ms", "stddev_ms", "throughput_mbps"],
      "additionalProperties": false,
      "properties": {
        "repeat":          { "type": "integer", "minimum": 1 },
        "times_ms":        { "type": "array", "items": { "type": "number", "minimum": 0 }, "minItems": 1 },
        "mean_ms":         { "type": "number", "minimum": 0 },
        "min_ms":          { "type": "number", "minimum": 0 },
        "stddev_ms":       { "type": "number", "minimum": 0 },
        "throughput_mbps": { "type": "number", "minimum": 0 }
      }
    },
    "system": {
      "type": "object",
      "required": ["cpu_count", "platform", "timestamp_utc"],
      "additionalProperties": false,
      "properties": {
        "cpu_count":     { "type": "integer", "minimum": 1 },
        "platform":      { "type": "string" },
        "timestamp_utc": { "type": "string", "format": "date-time" },
        "ram_gb":        { "type": "number" },
        "cpu_model":     { "type": "string" }
      }
    }
  },
  "if": {
    "properties": { "status": { "const": "skipped" } },
    "required": ["status"]
  },
  "then": {
    "required": ["skip_reason"]
  },
  "else": {
    "required": ["parameters", "results", "system"]
  }
}
