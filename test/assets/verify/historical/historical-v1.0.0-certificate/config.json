{
  "description": "Backwards compatibility: certificate-signed bundle from Go v1.0.0 (first stable release)",
  "method": "certificate",
  "model_path": "model",
  "verify": {
    "cert_chain": [
      "keys/certificate/ca-cert.pem"
    ]
  }
}
