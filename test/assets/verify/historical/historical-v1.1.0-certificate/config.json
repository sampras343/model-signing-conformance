{
  "description": "Backwards compatibility: certificate-signed bundle from Go v1.1.0 (with ignore_paths in predicate)",
  "method": "certificate",
  "model_path": "model",
  "ignore_paths": ["ignore-me"],
  "cert_chain": ["keys/certificate/ca-cert.pem"]
}
