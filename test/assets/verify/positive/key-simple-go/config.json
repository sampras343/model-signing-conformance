{
  "description": "Go-signed key-based verification (cross-client interop: Go signs, test client verifies)",
  "method": "key",
  "model": "models/simple",
  "verify": {
    "public_key": "keys/certificate/signing-key-pub.pem",
    "ignore_paths": ["ignore-me"]
  }
}
