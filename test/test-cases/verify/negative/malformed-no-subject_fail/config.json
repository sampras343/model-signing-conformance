{
  "description": "FAIL: Rejection of bundle with empty subject array (\u00a76.5 \u2014 subject MUST contain exactly one entry)",
  "method": "key",
  "model": "models/simple",
  "expect": "fail",
  "verify": {
    "public_key": "keys/certificate/signing-key-pub.pem",
    "ignore_paths": [
      "ignore-me"
    ]
  }
}