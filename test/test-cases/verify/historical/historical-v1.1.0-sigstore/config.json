{
  "description": "Backwards compatibility: sigstore-signed bundle from Python v1.1.0 (Fulcio cert + Rekor transparency log)",
  "method": "sigstore",
  "model": "model",
  "model_relative_to": "test_dir",
  "expected_signed_files": ["signme-1", "signme-2"],
  "verify": {
    "ignore_paths": [
      "ignore-me",
      "model.sig"
    ]
  }
}
