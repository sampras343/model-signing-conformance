{
  "description": "Keyless sigstore sign-then-verify with --ignore-paths (§4.1, §6.2)",
  "method": "sigstore",
  "model": "models/simple",
  "requires_ci": true,
  "sign": {
    "identity_token_env": "SIGSTORE_ID_TOKEN"
  },
  "verify": {
    "identity": "${SIGSTORE_IDENTITY}",
    "identity_provider": "${SIGSTORE_ISSUER}",
    "ignore_paths": ["ignore-me"]
  },
  "expected_signed_files": ["signme-1", "signme-2"]
}
